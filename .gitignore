# Secrets
.env

# Operator-specific config (committed: *.example.yaml)
config/filters.yaml
config/notion.yaml
config/places.yaml

# SQLite database — contains scraped data; treat as private
aptos.db
aptos.db-journal
aptos.db-wal
aptos.db-shm

# Python / uv
.venv/
__pycache__/
*.pyc
*.pyo
*.egg-info/
build/
dist/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Playwright persistent context (session token equivalent)
# Note: actual location is ~/.cache/aptos-sp/playwright-profile/ — gitignored by being outside the repo

# OS / editor
.DS_Store
*.swp
.vscode/
.idea/
